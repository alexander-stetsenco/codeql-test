---
scan_execution_policy:
- name: default
  description: ''
  enabled: true
  rules:
  - type: pipeline
    branches:
    - "*"
  actions:
  - scan: secret_detection
  - scan: sast
  - scan: dependency_scanning
approval_policy: []
